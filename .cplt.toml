# Generated by `cplt init` — review before trusting.
# Detected: JVM (Gradle)
# Workspace members:
#   kontrakter-felles (settings.gradle) — JVM (Gradle)
#   pdl-personhendelser-avro (settings.gradle) — JVM (Gradle)

[propose]
# Suggested by: (root), kontrakter-felles, pdl-personhendelser-avro
allow_jvm_attach = true

# The following are machine-specific — add to ~/.config/cplt/config.toml:
# allow.read += ["~/.gradle/gradle.properties"]  # in personal config

